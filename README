'rudi-pipelines-framework' carries support scripts used by all pipelines

job_manager  utilities that execute 'rudi submit' and related commands,
             i.e.,  job submission to a cluster server job scheduler

pipeline     utilities that enable pipeline executables;
             thus, 'pipeline' scripts are called by 'job_manager' scripts

shell        custom shell utilities available to all running pipelines
